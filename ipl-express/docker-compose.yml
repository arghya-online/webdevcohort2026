services:
  app:
    image: node:22-alpine
    container_name: ipl-express-app
    working_dir: /app
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
    command: sh -c "npm install && node index.js"
